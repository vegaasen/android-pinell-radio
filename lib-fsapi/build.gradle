apply plugin: 'java'

sourceCompatibility = 1.7
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral();
}

dependencies {
    compile project(':lib-rest-utils')
    compile project(':lib-telnet')
    testCompile group: 'junit', name: 'junit', version: '4.11';
}